diff --git a/packages/client/ui-conversation/src/client/skeleton/TodoPanel.module.css b/packages/client/ui-conversation/src/client/skeleton/TodoPanel.module.css
index e67037db7d..e5e1a17fc9 100644
--- a/packages/client/ui-conversation/src/client/skeleton/TodoPanel.module.css
+++ b/packages/client/ui-conversation/src/client/skeleton/TodoPanel.module.css
@@ -108,6 +108,10 @@
   font-size: 13px;
   line-height: 20px;
   color: var(--dsw-alias-label-secondary);
+  /* Nested rows (the tree tool's shape) inset by depth. The fallback keeps a
+     flat plan's rows at their exact figma inset, since a flat item carries no
+     depth variable at all. */
+  padding-inline-start: calc(var(--dsw-todo-depth, 0) * 18px);
 }
 
 .glyph {
diff --git a/packages/client/ui-conversation/src/client/skeleton/TodoPanel.tsx b/packages/client/ui-conversation/src/client/skeleton/TodoPanel.tsx
index ad627e7cad..cbef8c6ef1 100644
--- a/packages/client/ui-conversation/src/client/skeleton/TodoPanel.tsx
+++ b/packages/client/ui-conversation/src/client/skeleton/TodoPanel.tsx
@@ -13,13 +13,49 @@ import type { PropsLocale, PropsRuntime } from '@deepseek-ai/dsh-client-ui-slots
 // declare) and the payload type. Type-only by construction — the outlet is
 // free of host value imports, so no host Context merge enters this program.
 import type { TodoItem } from '@deepseek-ai/dsh-tool-todo/client'
+// The nested tool's matching outlet: it merges the `todoTree` projection key and
+// carries `TodoTreeItem`. Exactly one of the two todo tools mounts, so exactly
+// one of the two keys is ever present.
+import type { TodoTreeItem } from '@deepseek-ai/dsh-tool-todo-tree/client'
 import { IconChecklistOutline14, IconChevronDownOutline14, IconChevronUpOutline14 } from '@deepseek-ai/dsh-client-ui-primitives'
 import { NS } from '../locales.ts'
 import css from './TodoPanel.module.css'
 
+/**
+ * One rendered row: a plan node plus the depth it sits at. A flat plan yields
+ * only depth-0 rows, so it renders exactly as before.
+ */
+interface TodoRow {
+  item: TodoItem | TodoTreeItem
+  depth: number
+}
+
+/**
+ * Flatten a plan into display-ordered rows carrying their depth. The flat tool's
+ * items have no `children`, so this is the identity for a flat plan.
+ *
+ * Iterative rather than recursive: the projection payload is host-validated, but
+ * the panel also renders it during replay of logs this build did not write, and
+ * an unbounded tree must not become a render-killing RangeError.
+ * @param todos - the plan, in model order.
+ * @returns each node with its nesting depth, parents before their children.
+ */
+function toRows(todos: readonly (TodoItem | TodoTreeItem)[]): TodoRow[] {
+  const rows: TodoRow[] = []
+  const stack: TodoRow[] = [...todos].reverse().map(item => ({ item, depth: 0 }))
+  for (let row = stack.pop(); row !== undefined; row = stack.pop()) {
+    rows.push(row)
+    const children = 'children' in row.item ? row.item.children : undefined
+    if (children !== undefined) {
+      for (const child of [...children].reverse()) stack.push({ item: child, depth: row.depth + 1 })
+    }
+  }
+  return rows
+}
+
 export interface TodoPanelProps {
   /** The session's current plan (empty renders nothing) — selected by the dock adapter. */
-  todos: readonly TodoItem[]
+  todos: readonly (TodoItem | TodoTreeItem)[]
   /** The dock entry's locale seat, passed down as a plain prop. */
   t: TodoDockProps['t']
 }
@@ -78,11 +114,12 @@ function StatusGlyph({ status }: { status: TodoItem['status'] }) {
   }
 }
 
-/** Header summary: "·"-joined per-status counts; zero-count segments are omitted as noise (a non-empty list keeps at least one). */
-function progressLabel(todos: readonly TodoItem[], t: TodoPanelProps['t']): string {
-  const done = todos.filter(item => item.status === 'completed').length
-  const active = todos.filter(item => item.status === 'in_progress').length
-  const pending = todos.length - done - active
+/** Header summary: "·"-joined per-status counts over every depth; zero-count
+ *  segments are omitted as noise (a non-empty plan keeps at least one). */
+function progressLabel(rows: readonly TodoRow[], t: TodoPanelProps['t']): string {
+  const done = rows.filter(row => row.item.status === 'completed').length
+  const active = rows.filter(row => row.item.status === 'in_progress').length
+  const pending = rows.length - done - active
   // En spaces (U+2002): HTML collapses runs of ASCII spaces, so widening the
   // separator breathing room needs a literal wide space.
   return [
@@ -95,6 +132,7 @@ function progressLabel(todos: readonly TodoItem[], t: TodoPanelProps['t']): stri
 export function TodoPanel({ todos, t }: TodoPanelProps) {
   const [collapsed, setCollapsed] = useState(true)
   if (todos.length === 0) return null
+  const rows = toRows(todos)
 
   return (
     <section className={css.root} data-testid="todo-panel" aria-label={t('todo.title')}>
@@ -107,17 +145,25 @@ export function TodoPanel({ todos, t }: TodoPanelProps) {
         >
           <span className={css.lead} aria-hidden><IconChecklistOutline14 /></span>
           <span className={css.title}>{t('todo.title')}</span>
-          <span className={css.progress}>{progressLabel(todos, t)}</span>
+          <span className={css.progress}>{progressLabel(rows, t)}</span>
           <span className={css.chevron} aria-hidden>
             {collapsed ? <IconChevronUpOutline14 /> : <IconChevronDownOutline14 />}
           </span>
         </button>
         {!collapsed && (
           <ul className={css.list}>
-            {todos.map(item => (
-              <li key={item.content} className={css.item} data-status={item.status}>
-                <span className={css.glyph} aria-hidden><StatusGlyph status={item.status} /></span>
-                <span className={css.content}>{item.content}</span>
+            {rows.map((row, index) => (
+              // Content is unique only among siblings in a tree, so the row
+              // index carries the identity a repeated child name would break.
+              <li
+                key={`${index}:${row.item.content}`}
+                className={css.item}
+                data-status={row.item.status}
+                data-depth={row.depth}
+                style={{ '--dsw-todo-depth': row.depth } as React.CSSProperties}
+              >
+                <span className={css.glyph} aria-hidden><StatusGlyph status={row.item.status} /></span>
+                <span className={css.content}>{row.item.content}</span>
               </li>
             ))}
           </ul>
@@ -130,10 +176,17 @@ export function TodoPanel({ todos, t }: TodoPanelProps) {
 /** Full props of a dock entry: InputZone owner share + session standard kit + global seat + the locale seat. */
 export type TodoDockProps = PropsRuntime<'conversation.input.dock'> & PropsLocale<'conversation'>
 
-/** Dock adapter: reads the host-computed 'todos' projection (whole list; absent or null renders nothing). */
+/**
+ * Dock adapter: reads the host-computed plan projection (whole plan; absent or
+ * null renders nothing). Exactly one todo tool mounts per deployment, so at most
+ * one of the two keys is present — the flat tool publishes `todos`, the nested
+ * one `todoTree`. Reading both keeps the panel working under either composition
+ * without asking the host which is live.
+ */
 export function TodoDock({ useProjection, t }: TodoDockProps) {
   const todos = useProjection('todos')
-  return <TodoPanel todos={todos ?? []} t={t} />
+  const todoTree = useProjection('todoTree')
+  return <TodoPanel todos={todos ?? todoTree ?? []} t={t} />
 }
 
 /**
diff --git a/packages/client/ui-conversation/tests/todo-panel.client.spec.tsx b/packages/client/ui-conversation/tests/todo-panel.client.spec.tsx
index fafe6e6dc4..6311c417ae 100644
--- a/packages/client/ui-conversation/tests/todo-panel.client.spec.tsx
+++ b/packages/client/ui-conversation/tests/todo-panel.client.spec.tsx
@@ -9,6 +9,7 @@ import { afterEach, describe, expect, it, vi } from 'vitest'
 import { bindSnapshotSelector } from '@deepseek-ai/dsh-client-web-react'
 import { createSnapshotStore } from '@deepseek-ai/dsh-client-runtime/client'
 import type { TodoItem } from '@deepseek-ai/dsh-client-runtime/client'
+import type { TodoTreeItem } from '@deepseek-ai/dsh-tool-todo-tree/client'
 import { makeTranslate } from '@deepseek-ai/dsh-client-test-runtime'
 import { zh as commonZh } from '@deepseek-ai/dsh-client-locale/src/locales/zh.ts'
 import type { TodoDockProps } from '../src/client/skeleton/TodoPanel.tsx'
@@ -102,18 +103,82 @@ describe('TodoPanel', () => {
     expect(screen.getByText('1 已完成')).toBeTruthy()
     expect(screen.queryByText(/进行中|待处理/)).toBeNull()
   })
+
+  it('expands a nested plan with one indented row per node at every depth', () => {
+    render(<TodoPanel
+      todos={[{
+        content: '实现修复',
+        status: 'in_progress',
+        children: [
+          { content: '读代码', status: 'completed' },
+          { content: '改模块', status: 'pending', children: [{ content: '改函数', status: 'pending' }] },
+        ],
+      }]}
+      t={t}
+    />)
+    fireEvent.click(screen.getByRole('button'))
+    // Rows come out in display order (pre-order) and carry their depth.
+    const rows = screen.getAllByRole('listitem')
+    expect(rows.map(row => [row.textContent, row.dataset.depth])).toEqual([
+      ['实现修复', '0'],
+      ['读代码', '1'],
+      ['改模块', '1'],
+      ['改函数', '2'],
+    ])
+  })
+
+  it('counts every depth in the header summary', () => {
+    render(<TodoPanel
+      todos={[{
+        content: '根',
+        status: 'in_progress',
+        children: [
+          { content: '完成的子任务', status: 'completed' },
+          { content: '待办的子任务', status: 'pending' },
+        ],
+      }]}
+      t={t}
+    />)
+    // A top-level-only count would read "1 进行中" alone and hide both children.
+    expect(screen.getByText('1 已完成 · 1 进行中 · 1 待处理')).toBeTruthy()
+  })
+
+  it('keeps repeated child names as separate rows', () => {
+    // Content is unique only among siblings, so the same name recurs across groups.
+    render(<TodoPanel
+      todos={[
+        { content: 'A', status: 'pending', children: [{ content: '跑测试', status: 'pending' }] },
+        { content: 'B', status: 'pending', children: [{ content: '跑测试', status: 'pending' }] },
+      ]}
+      t={t}
+    />)
+    fireEvent.click(screen.getByRole('button'))
+    expect(screen.getAllByText('跑测试')).toHaveLength(2)
+    expect(screen.getAllByRole('listitem')).toHaveLength(4)
+  })
 })
 
-/** Dock props stub: the adapter reads the 'todos' projection only; the rest of the owner share is unused. */
-function dockProps(store: ReturnType<typeof createSnapshotStore<{ value: readonly TodoItem[] | null | undefined }>>): TodoDockProps {
-  const useProjection = (_key: string, selector?: (v: unknown) => unknown) =>
-    bindSnapshotSelector(store)(s => (selector ?? (v => v))(s.value))
+/** Either plan shape as a projection value: absent (no frame yet), null (pre-first-write), or the whole plan. */
+type PlanValue = readonly (TodoItem | TodoTreeItem)[] | null | undefined
+
+/**
+ * Dock props stub: the adapter reads the two plan projections, so the stub
+ * answers per key — a key-blind stub would hand the same store to both reads and
+ * hide which one the adapter actually consulted. The rest of the owner share is
+ * unused.
+ */
+function dockProps(
+  store: ReturnType<typeof createSnapshotStore<{ value: PlanValue }>>,
+  key: 'todos' | 'todoTree' = 'todos',
+): TodoDockProps {
+  const useProjection = (requested: string, selector?: (v: unknown) => unknown) =>
+    bindSnapshotSelector(store)(s => (selector ?? (v => v))(requested === key ? s.value : undefined))
   return { useProjection, t } as unknown as TodoDockProps
 }
 
 describe('TodoDock', () => {
   it('reads the host-computed todos projection and follows pushed updates', () => {
-    const store = createSnapshotStore<{ value: readonly TodoItem[] | null | undefined }>({ value: undefined })
+    const store = createSnapshotStore<{ value: PlanValue }>({ value: undefined })
     render(<TodoDock {...dockProps(store)} />)
     // Capability absent (no baseline/frame yet) renders nothing.
     expect(screen.queryByTestId('todo-panel')).toBeNull()
@@ -124,6 +189,22 @@ describe('TodoDock', () => {
     expect(screen.queryByTestId('todo-panel')).toBeNull()
   })
 
+  it('reads the nested todoTree projection when that tool is the mounted one', () => {
+    // Only the nested key answers here: the flat `todos` key is absent, exactly
+    // as in a deployment that mounted the tree tool instead.
+    const store = createSnapshotStore<{ value: PlanValue }>({ value: undefined })
+    render(<TodoDock {...dockProps(store, 'todoTree')} />)
+    expect(screen.queryByTestId('todo-panel')).toBeNull()
+    act(() => {
+      store.set({ value: [
+        { content: '根', status: 'in_progress', children: [{ content: '子', status: 'completed' }] },
+      ] })
+    })
+    expect(screen.getByText('1 已完成 · 1 进行中')).toBeTruthy()
+    fireEvent.click(screen.getByRole('button'))
+    expect(screen.getAllByRole('listitem').map(row => row.dataset.depth)).toEqual(['0', '1'])
+  })
+
   it('registers before the goal and queue entries', () => {
     expect(todoDockEntry.name).toBe('conversation-todo-dock')
     expect(todoDockEntry.inject).toEqual(['slots'])
diff --git a/packages/client/ui-conversation/tsconfig.json b/packages/client/ui-conversation/tsconfig.json
index 79e9e3dc7c..7abb8f7d23 100644
--- a/packages/client/ui-conversation/tsconfig.json
+++ b/packages/client/ui-conversation/tsconfig.json
@@ -62,6 +62,9 @@
     {
       "path": "../../todo/tool-todo"
     },
+    {
+      "path": "../../todo/tool-todo-tree"
+    },
     {
       "path": "../ui-input-trigger"
     },
